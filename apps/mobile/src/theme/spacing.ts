/** Theme-adjacent layout scale (Phase 1). */
export { space } from '../ui/spacing';

export const controlSizes = {
  side: 64,
  hear: 64,
  replay: 64,
  mic: 98,
  navHeight: 52,
  topIcon: 42,
} as const;
